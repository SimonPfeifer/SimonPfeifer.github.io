---
import AstroPost from '../../layouts/AstroPost.astro';

import {frontmatter as post} from '../../content/astro/astro.md';
import {Content} from '../../content/astro/astro.md';
---

<AstroPost 
  title={post.title}
  description={post.description}
  pubDate={post.pubDate}
  heroImage={post.heroImage}
  active={true}
>
	<Content />
</ProjectPost>

